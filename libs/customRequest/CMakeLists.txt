add_library(libcustomRequest SHARED  src/customRequest.cpp)
target_include_directories(libserver PUBLIC include)
target_link_libraries(libcustomRequest PUBLIC  libclient libdata_struct libserialization)